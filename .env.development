# Local development defaults (loaded by Vite in `dev` mode only).
# Never loaded by `vite build` (production), so these stay out of the bundle.

# Enable the passwordless demo session for local exploration and smoke tests.
# There is no demo credential — team login accepts any input in this mode.
VITE_DEMO_MODE=true
