# F1.1 — Real Data Integrity — Completion Report

Sprint: F1.1 — Real Data Integrity
Status: ✅ Complete
Scope: Demo mode only (`FEATURES.BACKEND_INTEGRATION = false`).

---

## Executive Summary

In demo mode the app rendered fabricated data that did not survive a refresh
and, for Analytics, changed on every reload. The root cause was that demo-mode
data was generated fresh (some of it randomly) on each read and never
persisted. This sprint introduces a small **localStorage-backed demo
persistence store** seeded once from the existing demo constants, and routes
the demo branches of the data service through it. All six issues are fixed
without touching the backend path, the Storage Gateway, KV/SQL authority, API
contracts, or any layout.

---

## Root Causes & Fixes

1. **Dummy/hardcoded data** — Multiple modules generated or hardcoded their own
   demo data. Consolidated onto one persisted demo store; removed the divergent
   copies (see items below).

2. **Dashboard did not display persisted questionnaire answers** — In demo mode
   `DiagnosticRoute` never called `createSubmission`, so answers were discarded;
   and the CORTEX detail view always used static mock data.
   Fix: `createSubmission` now runs in demo mode and persists the full
   submission (with answers) to the demo store; `CortexDashboard` builds its
   view from the persisted submission's real answers (`generateCortexData`) when
   present, falling back to mock only when there are none.

3. **Analytics regenerated values on refresh** — `AnalyticsDashboard` built 20
   submissions with `Math.random()` scores/dates on every load.
   Fix: removed the random generator; Analytics now reads the persisted demo
   submissions (stable across refreshes) and computes analytics from them.

4. **Platform Health used fabricated counts** — `SettingsPage` hardcoded
   `{ new:5, in-review:3, completed:8, approved:2, total:18 }`.
   Fix: counts are derived from the persisted submissions (real data / graceful
   zero-state); the data-service demo `getPlatformSettings` was aligned too.

5. **Email Queue injected fake demo leads** — `EmailNurturePanel` called
   `seedDemoQueue()` on every refresh, polluting the real queue.
   Fix: removed the seeding call; the queue now shows only real persisted emails
   (created when a diagnostic is submitted) with a proper empty state.

6. **Invited team members did not persist** — demo invite/update/remove only
   mutated local component state.
   Fix: the demo team roster is persisted; invite/update/remove write through to
   the store, so members survive a refresh.

---

## Files Modified

- `src/app/utils/demoData.ts` — added the localStorage-backed demo store
  (submissions + team roster) with seed builders and mutators.
- `src/app/services/dataService.ts` — demo branches persist/read via the store:
  `createSubmission`, `getAnalytics`, `getPlatformSettings`, `inviteTeamMember`,
  `updateTeamMember`, `removeTeamMember`.
- `src/app/pages/DiagnosticRoute.tsx` — persist the submission in demo mode too.
- `src/app/components/AnalyticsDashboard.tsx` — removed random generator; read
  persisted submissions.
- `src/app/components/SettingsPage.tsx` — Platform Health counts derived from
  persisted submissions.
- `src/app/components/EmailNurturePanel.tsx` — removed fake queue seeding;
  clearer empty state.
- `src/app/components/FullFeaturedDashboard.tsx` — leads list reads the
  persisted store (new submissions appear).
- `src/app/components/CortexDashboard.tsx` — demo detail view uses real
  persisted answers when available.

## Files Created

- `MARQ_CORTEX_STABILIZATION_ROADMAP.md`
- `architecture/F1.1-REAL-DATA-INTEGRITY-COMPLETION.md`

---

## Tests

- `npm run build` (vite) — passes.
- Smoke: `tests/smoke/diagnostic-score-team-login.spec.ts` — passes.
- Verification (run against a local dev server, then removed):
  - Analytics values stable across repeated refresh — pass.
  - Invited team member persists after reload — pass.
  - Submitted questionnaire persists with answers; email queue contains no
    fabricated demo leads — pass.

---

## Runtime Impact

- Backend/live path unchanged. Storage Gateway, KV/SQL authority, API contracts,
  DTOs, and response envelopes preserved.
- Demo mode now persists user-created data (submissions, team members) and
  renders stable, real values. No layout or console-error changes.
- Persistence keys: `marq_cortex_demo_submissions_v1`,
  `marq_cortex_demo_team_v1` (seed-once, versioned).

## Rollback

Revert the eight source files and delete the two new docs. The persistence keys
are additive; clearing them returns demo mode to seed state.

---

## QA Follow-up — MC-0002 (Dashboard did not display persisted diagnostic data)

**Reported:** After a diagnostic submission completes, the expected data did not
appear on the Dashboard.

**Root cause:** The default Dashboard view (Command Center) rendered a **static
hardcoded `ACTIVITY_FEED`** in the "Recent Activity" panel — fabricated events
(e.g. "Proposal draft §3 updated — TechCorp Solutions", "New diagnostic
submitted — FinanceHub"). It never reflected the persisted submissions, so a
freshly submitted diagnostic could not appear there. (The persisted submission
was already reaching the "All Leads" list and CORTEX detail; the gap was
specifically the default Command Center activity feed.)

**Fix (smallest path):** Replaced the static array with `buildActivityFeed(submissions)`,
which derives the feed from the real persisted submissions (newest first, with a
relative-time label) and renders a proper empty state when there are none. No
layout change; removed the hardcoded events (also advances issue #1).

**File:** `src/app/components/TeamHomeDashboard.tsx`

**Verification:** `vite build` passes; smoke test passes; a UI test confirmed a
submitted diagnostic appears in Recent Activity with a live "just now / min ago"
timestamp and that the old hardcoded strings are gone.

---

## Remaining Issues

None within F1.1 scope. `seedDemoQueue()` remains exported in
`emailNurtureQueue.ts` but is no longer invoked (dead utility, left in place to
avoid unrelated churn).

## Next Sprint

Do not start F1.2.
